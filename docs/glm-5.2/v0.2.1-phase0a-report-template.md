# GLM-5.2 V0.2.1 Task 0A OpenRouter 黑盒矩阵报告

> 本报告只记录测试 Key 的合成数据。不得粘贴 API Key、Authorization、生产用户 Prompt 或生产数据库内容。

## 1. 执行信息

- 执行人：艾隆
- 复核人：柯大侠
- 参数确认：建国
- 日期：待填写
- 代码分支：`feat/glm-5.2-v1.1`
- 起始提交：`7a0f776`
- 测试模型：`z-ai/glm-5.2`
- 脚本：`scripts/glm52-v021-phase0a.sh`
- 输出目录：待填写
- `run-manifest.json` SHA256：待填写
- `summary.csv` SHA256：待填写

## 2. 安全与费用边界

- [ ] 没有读取或复制生产用户数据。
- [ ] Key 只通过 `OPENROUTER_API_KEY` 环境变量提供。
- [ ] 输出文件中没有 Key 或 Authorization。
- [ ] 未设置 `provider.order`，顺应 OpenRouter 内部聚合与 fallback。
- [ ] 首次只运行 smoke。
- [ ] 每次真实运行都显式设置 `--max-cost-usd`。
- [ ] Task 0B 和生产 atmApi 未触碰。

脚本默认 dry-run，不会联网。建议执行顺序：

```bash
git status --short --branch
python3 scripts/test_glm52_v021_phase0a.py
bash scripts/glm52-v021-phase0a.sh --profile smoke

export OPENROUTER_API_KEY='仅在当前 shell 设置，不写入文件'
bash scripts/glm52-v021-phase0a.sh \
  --profile smoke \
  --execute \
  --acknowledge-cost I_UNDERSTAND_OPENROUTER_COSTS \
  --max-cost-usd 1.00
```

Smoke 结果复核、脚本更新并获得建国新的费用授权后，才可运行 core-safe：

```bash
bash scripts/glm52-v021-phase0a.sh \
  --profile core-safe \
  --max-cost-usd 4.00

# 只有建国授权后才添加 --execute：
bash scripts/glm52-v021-phase0a.sh \
  --profile core-safe \
  --execute \
  --acknowledge-cost I_UNDERSTAND_OPENROUTER_COSTS \
  --max-cost-usd 4.00 \
  --sleep-seconds 1
```

`core-safe` 共32例，按保守单价 dry-run 最坏估算 `$3.076005`，完全排除 xhigh；`extended` 只有建国确认 core-safe 结果和额外费用后才运行。

## 3. 口径说明

- high≈80%、xhigh≈95% 只是输出预算规划上界，不是实际 reasoning 占比预测或 GLM-5.2 服务承诺。
- `ttft_ms` 只有流式用例表示真实首个 SSE data event；非流式标记为 `full_response`。
- `first_reasoning_ms` 与 `first_visible_ms` 分别记录首个推理事件和首个用户可消费结果，二者不能混为同一个 TTFT。
- `visible_tokens = completion_tokens - reasoning_tokens` 是 usage 口径推导值。
- `observed_terminal_kind` 根据 `content/tool_calls/refusal/reasoning_only/empty` 汇总黑盒证据，但 Task 0A 不替代 Task 4 的正式终态分类器。
- `saw_done/stream_complete/terminal_valid` 分别表示是否收到 `[DONE]`、协议流是否完整、是否存在用户可消费终态；HTTP 200 本身不代表成功。
- `estimated_worst_cost_usd` 只用于运行前费用护栏，最终成本以 `usage.cost` 或 OpenRouter 账单为准。
- Smoke 的缓存命中只证明缓存存在，不能归因于 `session_id`。core-safe 使用三条独立 Prompt 命名空间，对 shared/unique/omitted session 各重复两次。
- xhigh 的 reasoning-only 且缺失 `[DONE]` 只证明复现了空响应症状；没有 usage 时不能断言 reasoning 耗尽了 max_tokens。

## 4. Smoke 结果

| case_id | HTTP | prompt/cached | completion (reasoning/visible) | terminal | finish | first reasoning ms | first visible ms | cost USD | 结论 |
|---|---:|---|---|---|---|---:|---:|---:|---|
| ctrl_default_h12_m8k_s |  |  |  |  |  |  |  |  |  |
| ctrl_high_h12_m8k_s |  |  |  |  |  |  |  |  |  |
| ctrl_xhigh_h12_m8k_s |  |  |  |  |  |  |  |  |  |

Smoke Go/No-Go：待填写。

## 5. Core-Safe 成对结果

从 `summary.csv` 按 history/max_tokens 分组，至少填写：

| history | max_tokens | 样本 | default reasoning p50/p95 | high reasoning p50/p95 | default visible min | high visible min | incomplete 次数 | first visible p95 | cost 合计 |
|---:|---:|---:|---|---|---:|---:|---:|---:|---:|
| 50 | 8192 |  |  |  |  |  |  |  |  |
| 50 | 16384 |  |  |  |  |  |  |  |  |
| 50 | 32768 |  |  |  |  |  |  |  |  |
| 100 | 8192 |  |  |  |  |  |  |  |  |
| 100 | 16384 |  |  |  |  |  |  |  |  |
| 100 | 32768 |  |  |  |  |  |  |  |  |
| 200 | 8192 |  |  |  |  |  |  |  |  |
| 200 | 16384 |  |  |  |  |  |  |  |  |
| 200 | 32768 |  |  |  |  |  |  |  |  |
| 300 | 8192 |  |  |  |  |  |  |  |  |
| 300 | 16384 |  |  |  |  |  |  |  |  |
| 300 | 32768 |  |  |  |  |  |  |  |  |

## 6. reasoning.max_tokens 支持性

- 请求是否被 OpenRouter 接受：待填写。
- 实际 reasoning token 是否小于等于 4096：待填写。
- 不同 Provider 是否行为一致：待填写。
- 是否可以作为 Task 3 的硬保底：待填写。
- 原始 case：`ctrl_rmax4k_h12_m8k_n`。

## 7. 缓存与 OpenRouter 聚合观察

- 三组必须比较各自第1次与第2次，不能跨 Prompt 命名空间互相预热。

| arm | session模式 | 第1次 cached/prompt | 第2次 cached/prompt | Provider 1/2 | TTFT 1/2 | 费用1/2 | 结论 |
|---|---|---|---|---|---|---|---|
| cache-shared | 相同 session_id |  |  |  |  |  |  |
| cache-unique | 不同 session_id |  |  |  |  |  |  |
| cache-omitted | 不传 session_id |  |  |  |  |  |  |

- 响应是否暴露 Provider：待填写。
- shared 相对 unique/omitted 是否产生可重复的增益：待填写。
- 是否发现 `session_id` 或 `usage.include` 不兼容：待填写。

## 8. Task 3 参数建议

| 项目 | 建议值 | 证据 |
|---|---|---|
| reasoning 未启用 | 保持未启用 |  |
| `enabled=true` 未指定 effort | 候选：规范为 high |  |
| high 可见输出保留量 | 待定 |  |
| `reasoning.max_tokens` | 待定 |  |
| xhigh | V0.2.1 首版仍返回 400 | 三方已签字；只有新证据和建国确认才能改变 |
| basic/standard/pro max_tokens | 待定 |  |

## 9. 异常与原始证据

- HTTP 4xx/5xx：待填写。
- reasoning-only：待填写。
- 流中断或缺失 `[DONE]`：待填写。
- usage/cost 缺失：待填写。
- 其他：待填写。

原始证据只保存在服务器测试目录；在飞书中只放脱敏汇总和 SHA256，不上传完整模型输出。

## 10. 签字

- 艾隆（执行）：待确认。
- 柯大侠（分析）：待确认。
- 建国（Task 3 参数）：待确认。
- Task 3 状态：继续冻结，直到本报告完成并由建国确认。
